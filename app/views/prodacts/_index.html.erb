<% if admin_product.is_active == false %>
 <%= link_to "Approve", prodacts_updatestatus_path( p_id: admin_product.id), method: :post ,remote: true  %>
<% else %>
 <%= link_to "Disapprove", prodacts_updatestatus_path( p_id: admin_product.id), method: :post ,remote: true  %>
<% end %>
