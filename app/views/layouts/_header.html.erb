<header id="header">
  <div class="header-middle">
    <div class="container">
      <div class="row">
        <div class="col-sm-4">
          <% if user_signed_in? %>
          <% if current_user.role == "admin" %>
          <h3><a href="/" style=" text-decoration: none !important"> Welcome To E-Fruit-Seller</a></h3>
          <% else%>
            <h3><a href="/prodacts" style=" text-decoration: none !important"> Welcome To E-Fruit-Seller</a></h3>
          <%end%>
          <%end%>
          <% if user_signed_in? %>
          <% if current_user.role == "admin" %>
          <ul class="nav nav-pils">
              <li ><a href="/">Home</a></li>
              <li><a href="/prodacts">Manage Product</a></li>
          </ul>
          <%end%>
          <%end%>
        </div>
        <div class="col-sm-8">
          <div class="shop-menu pull-right">
            <ul class="nav navbar-nav">
                <% if user_signed_in? %>
              <ul class="nav navbar-nav pull-right">
                <% if current_user.role == "user" %>
                <li><a href="/charges/new"> Checkout</a></li>
              <li><a href="/cart"><h1 class="cart-text"><%= render 'layouts/cart_text' %></h1> </a></li>
              <% end%>
                  <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                      <%= current_user.firstname.titleize %>
                      <span class="caret"></span>
                    </a>
                    <ul class="dropdown-menu" role="menu">
                      <li><%= link_to 'Profile', edit_user_registration_path %></li>
                      <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
                    </ul>
                  </li>
                <% else %>
          <% end %>
              </ul>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div><!--/header-middle-->

  <div class="header-bottom"><!--header-bottom-->
    <div class="container">
      <div class="row">
        <div class="col-sm-9">
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </div>


            </ul>
          </div>

        </div>

        <div class="col-sm-3">

        </div>
      </div>
    </div>
  </div><!--/header-bottom-->
</header><!--/header-->
